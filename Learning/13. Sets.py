# Set is a unordered mutable datatype which is use to store data but their elements present on set is immutable and it ignore the dublicate values.
collaction = set() # How define the Empty Set
collaction = {1, 2, 3, 4, 4, "Hello world", "Roaunak Gupta", (3, 4, 3)}
print(collaction)
print(type(collaction))
print(len(collaction))

